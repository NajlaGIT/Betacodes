# ── Staging / Beta Vision Environment ──────────────────────────────────────
BASE_URL=https://sstag.beta-vision.com   # <-- update to your actual sstag URL

# Test credentials  (never commit real values – use a secrets manager in CI)
TEST_USER_EMAIL=testuser@example.com
TEST_USER_PASSWORD=YourStagingPassword!

# Browser
HEADLESS=false          # set to true in CI
IMPLICIT_WAIT=10        # seconds
PAGE_LOAD_TIMEOUT=30    # seconds
